spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

spring.jpa.properties.hibernate.format_sql=true
server.port=${SERVER_PORT}

# SWAGGER:
# spring.mvc.pathmatch.matching-strategy=ANT_PATH_MATCHER
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Define swagger-ui path:
springdoc.swagger-ui.path=/api/v1/doc/swagger-ui.html

# Select packages, classes and scan:
#springdoc.packages-to-scan=com.package1
#springdoc.paths-to-match=/api/v1/*

# Learn more in https://spingdoc.org/


pokeapi.base-url=https://pokeapi.co/api/v2/pokemon